#don't re-download the data. Maybe not best practices but i changed SAS scripts slightly b/c they were coded weird and contained duplicates
#source("download data.R")

source("3- add column names.R")

source("4 - setup weights & srvy objs.R")

source("renaming hvs values.R")
